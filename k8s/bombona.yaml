apiVersion: apps/v1
kind: Deployment
metadata:
  name: bombona
spec:
  replicas: 1
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: bombona
  template:
    metadata:
      labels:
        app: bombona
    spec:
      serviceAccountName: andrusdiaz-home
      securityContext:
        runAsNonRoot: true
        runAsUser: 101
        runAsGroup: 101
        fsGroup: 101
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: nginx
          image: bombona
          ports:
            - name: http
              containerPort: 8080
          resources:
            requests:
              cpu: 10m
              memory: 24Mi
            limits:
              cpu: 100m
              memory: 96Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]
          readinessProbe:
            httpGet:
              path: /
              port: http
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir:
            sizeLimit: 16Mi
---
apiVersion: v1
kind: Service
metadata:
  name: bombona
spec:
  selector:
    app: bombona
  ports:
    - name: http
      port: 80
      targetPort: http
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bombona
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-graceandfavorvzla-services
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - bombona.andrusdiaz.dev
      secretName: bombona-tls
  rules:
    - host: bombona.andrusdiaz.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: bombona
                port:
                  name: http
